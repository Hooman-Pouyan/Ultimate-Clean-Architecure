import { InjectionToken } from '@angular/core';
import { CoreCommonConfig } from '../models/config.model';

export const CORE_COMMON_CONFIG = new InjectionToken<CoreCommonConfig>('core.common.config');
