export enum DataType {
  Index = 'index',
  Text = 'text',
  Numeric = 'numeric',
  Datetime = 'datetime',
  Currency = 'currency',
  Boolean = 'boolean',
  Enum = 'enum',
  Reference = 'reference',
}