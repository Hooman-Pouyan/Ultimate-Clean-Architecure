import { AuthPermissionDirective } from './auth-permission.directive';

describe('AuthPermissionDirective', () => {
  it('should create an instance', () => {
    const directive = new AuthPermissionDirective();
    expect(directive).toBeTruthy();
  });
});
